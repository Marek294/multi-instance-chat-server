// const { events, EventEmitter } = require('../utils/EventEmitter')
// const EmailService = require('../services/email')

// EventEmitter.on(events.user.created, async user => {
//   await EmailService.sendEmail(user)
// })
